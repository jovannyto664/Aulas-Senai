const mensagemPromocional = "Aproveite as ofertas exclusivas do Black Friday Senai hoje!";
const limite = mensagemPromocional.length
if (limite <= 60) {
    console.log(`Total: ${limite} caracteres. Dentro do limite: true`);
} else {
    "limite de caracteres excedifo"
}